# D435i 头部相机手眼标定：采集、处理与 5 mm 验收方案

适用配置：头部 D435i、原始 RGB 1280×720、12×9 方格、标称格距 15 mm。

本文是现场执行与采集程序对接规范。实际采集代码在外部程序中；本文不表示仓库已经自动实现全部采集检查、内参验证、时间同步和验收功能。

## 1. 目标与基本原则

目标：在事先规定的工作距离、头部姿态和视野范围内，独立验证点的**最大三维位置误差不超过 5 mm**。同时报告 RMS、各坐标分量误差和失败数量。

必须区分以下结果：

| 结果 | 含义 | 是否能单独证明绝对定位精度 ≤5 mm |
|---|---|---|
| 图像重投影误差，单位 px | 相机模型与检测角点的符合程度 | 不能 |
| 训练集闭环误差，单位 mm | 求解结果对参与标定数据的拟合程度 | 不能 |
| 独立固定板一致性误差，单位 mm | 新姿态观测同一固定目标时的一致性 | 不能单独证明绝对精度 |
| 相对外部已知坐标的三维误差，单位 mm | 相对于外部参考真值的定位准确度 | 可用于所覆盖工况的绝对精度验收 |

5 mm 是待验证的目标，不是相机分辨率、图像数量或算法名称可以提前保证的结果。文中的数量和质量阈值是本项目的建议起点，不是厂商规格或通用保证。

整个流程优先使用相机 SDK 提供的内参：**先读取、再验证；只有确认需要时才重新标定内参。** 不要求一开始就采 40 张图重新拟合 K、D。

## 2. 本次固定配置与坐标约定

| 项目 | 要求 |
|---|---|
| RGB 分辨率 | 实际保存的原图为 1280×720 |
| RGB 帧率 | 建议 30 FPS，记录实际启用值 |
| 图像格式 | 原始 BGR8/RGB8 无损 PNG；明确编码；不保存截图代替原图 |
| 棋盘规格 | 横向 12 个方格、纵向 9 个方格 |
| 检测规格 | **11×8 内角点，共 88 个** |
| 格距 | 标称 15 mm，以实测为准 |
| 手眼形式 | EIH：相机固定在头部，随头部运动 |
| 建议基座 | `waist_yaw_Link` |
| 末端 | `head_pitch_Link` |
| 相机系 | RGB optical：X 向右、Y 向下、Z 向前 |
| 采集时固定部分 | 棋盘、机器人底座、腰部、升降柱与相机安装支架 |
| 机器人数据 | 实测关节状态，不用指令角代替 |
| 位姿平移单位 | 归档矩阵明确单位；导出给当前 C++ 程序时使用 mm |
| 角度单位 | 原始关节数据建议 rad；导出 CSV 的 RPY 使用 deg |
| CSV 的 RPY 约定 | 固定轴/外旋 XYZ，即 R = Rz(rz) Ry(ry) Rx(rx) |

如果实际板上“12×9”指的是内角点数量，而不是方格数量，必须先更正配置。本文按已经确认的 **12×9 方格**执行。

以 `waist_yaw_Link` 为基座时，采集全程它必须相对棋盘静止。最终若需要相对于 `base_link` 或机械臂的精度，还要验证中间运动学链、机械臂基座关系，以及必要时的 TCP；头部手眼结果不自动覆盖这些误差。

## 3. 总流程与采集数量

```text
确认原始分辨率、真实 K/D/畸变模型、棋盘尺寸和机械固定
    ↓
检查每格像素尺寸及图像清晰度
    ↓
读取 SDK 内参，采 10～15 张不同视角图片作初步验证
    ├─ 验证正常：冻结 SDK 内参
    └─ 存在异常：排查图像处理/检测/棋盘，必要时独立重标定并验证
    ↓
固定板和头部，做静止重复性测试
    ↓
棋盘固定，采约 45 个手眼训练姿态
    ↓
求解并冻结内参、手眼矩阵及处理规则
    ↓
采约 25 个独立验证姿态和回访姿态
    ↓
固定板一致性验收；按最终用途增加外部真值/深度点云验收
```

| 数据 | 推荐起点 | 是否必须 |
|---|---:|---|
| SDK 内参初步验证 | 10～15 个不同视角 | 建议执行 |
| 内参重标定训练图 | 约 40 张 | 仅在确认需要重标定时 |
| 内参重标定验证图 | 约 15 张独立新图 | 重标定时需要 |
| 静止测试 | 每个检查位置约 100 帧 | 建议执行 |
| 手眼训练 | 40～60 个独立姿态，先按 45 个准备 | 需要 |
| 手眼最终验证 | 约 25 个独立姿态 | 需要 |
| 回访检查 | 选择约 5 个姿态，稍后重新到达 | 建议执行 |
| 每个手眼姿态的原始帧 | 10～20 帧，逐帧配对实测状态 | 建议保留 |

一个姿态连续拍 20 帧，仍然只算一个独立姿态。同一姿态、同一段连续采集的帧不能拆分到训练集和验证集。

## 4. 采集前准备

### 4.1 机械与棋盘

- 棋盘固定在刚性、平整的底板上，不能只让软纸悬挂；避免鼓包、翘曲和反光。
- 相机支架紧固，线缆不能在转头时拉动相机。
- 手眼采集期间，棋盘与基座之间不能发生相对运动。
- 横向、纵向跨多个方格测量实际格距。例如跨 10 格测总长度，再除以 10；重复测量不同位置并记录测量工具和结果。
- 外围保留白边，检测时完整显示全部内角点。
- 标记棋盘的物理原点方向，标记放在棋盘有效图案外。后续核验每帧对应同一物理角点，避免编号翻转。
- 不用修改程序中的格距数值来人为缩小毫米残差。若实测格距需要小数毫米，后续求解程序必须支持相应精度。

### 4.2 图像与光照

- 等相机输出、亮度和机械状态稳定后再开始正式采集。
- 使用均匀照明，避免白格饱和、反光、阴影和明显频闪。
- 停止移动后拍摄，确认棋盘黑白边缘清楚，没有运动模糊。
- 在照明允许时保持稳定的曝光设置，并记录曝光、增益和自动曝光状态。
- 保存无缩放、无裁剪、无绘制标记的原始图像；角点可视化另存一份。
- 如果使用已去畸变图，必须明确记录其对应的新 K 和零畸变模型。不能与原图的 K、D 混用。

### 4.3 检查像素尺寸

近似正对相机时，单格像素宽度约为：

```text
square_pixels ≈ fx × square_mm / distance_mm
```

以 fx=1200 px、格距 15 mm 为**计算示例**，不是本机实测内参：

| 距离 | 单格像素宽度 |
|---:|---:|
| 1200 mm | 约 15 px |
| 900 mm | 约 20 px |
| 600 mm | 约 30 px |

采集设计上争取相邻角点间距约 20～30 px 或更多，整板达到数百像素。实际应测量检测到的水平、垂直相邻角点间距，特别关注倾斜后较窄的方向。

**1280×720 不保证 15 mm 方格在 1.2 m 处就有 20～30 px。** 使用 SDK 的真实 fx 和实际角点间距评估。如果固定棋盘和分辨率下目标太小，可适当缩短标定距离；但最终必须在实际工作距离验证，不能用近距离通过代替 1.2 m 通过。

## 5. 读取与初步验证 SDK 内参

### 5.1 读取内容

从已经启动的实际 **RGB 视频流 profile** 读取并归档：

- 相机型号、序列号，SDK/固件版本（可以获取时）。
- width、height、FPS、图像格式。
- fx、fy、ppx、ppy，以及组成的 K。
- SDK 畸变模型 `intr.model` 和系数 `intr.coeffs`。
- 图像是否原始、是否去畸变、是否裁剪或缩放。

不要读取深度相机的 K 来处理 RGB，不要继续使用旧的 640×480 内参，也不要简单把旧内参乘二。分辨率、裁剪范围和视频流必须与保存图片一致。

SDK 返回的不同畸变模型不一定都能直接作为 OpenCV 的五参数 Brown 模型使用。保存原始模型名称和系数，并确认处理模型兼容；不能为了方便统一写成 `plumb_bob` 或全零。

### 5.2 本仓库现状，需要与外部采集端核对

- [RealSenseCamera.py](src/camera/RealSenseCamera.py) 已默认使用 1280×720，并通过实际 RGB profile 读取焦距和主点。
- [run_realsense_viewer.py](src/camera/run_realsense_viewer.py) 的默认分辨率也已改为 1280×720。
- 当前 Python 包装器没有完整转存 SDK 畸变模型和系数；发布器仍将 `CameraInfo.d` 写成五个零、模型写成 `plumb_bob`。
- 因此，ROS 话题中看到五个零不能证明实际 SDK 畸变为零。采集端应直接读取或核对 SDK 原始值；确认本机确实为零时，零系数才有依据。
- 上述文件只是当前驱动/发布逻辑；外部采集程序还需要落实本文的时间戳、分组和质量记录。

### 5.3 初步验证，仅需 10～15 个不同视角

此阶段不需要机器人位姿，可以固定相机，由人移动棋盘。

图片应覆盖中心、四边和四角附近，包含正视及左右/上下倾斜，并有不同板大小；每次停止移动后保存。棋盘保持完整、清晰。

处理时固定 SDK 的 K、D，仅对每张图片求棋盘位姿，再计算角点重投影误差。记录每张图及总体的 RMS、最大误差、误差方向和画面位置分布。

可以把总体角点重投影 RMS 约 ≤0.5 px、每张图 RMS ≤1 px 作为初步排查目标。它们不是 5 mm 精度的换算条件，也不是必须重标定的唯一依据。

决策：

1. 图像和内参对应正确、误差合理且没有明显系统性分布：冻结 SDK 内参，继续手眼采集。
2. 有异常：先检查缩放/裁剪、畸变模型、角点质量、棋盘平整度和对应关系。
3. 排除上述因素后仍有持续系统性误差，再考虑独立重标定。

## 6. 可选步骤：独立重标定内参

仅在需要时执行。优先使用已有可靠出厂参数，自己重标定不一定更准确。

建议约 40 张训练图，加约 15 张独立验证图：

- 覆盖画面中心、四边和四角附近。
- 有正视、左右倾斜、上下倾斜和组合倾斜；图像清楚时，可先以约 15°～35°倾角作为起点。
- 有不同距离和棋盘成像大小；不要所有图都近似平行、同位置、同大小。
- 连拍的相似帧不能代替不同视角。

仅用训练图拟合 K、D，使用有依据的相机模型。验证时固定 K、D，只求每张验证图的板位姿。保留 SDK 参数与重标定参数各自的验证结果和变化幅度。

不要仅根据训练像素误差下降就采用新内参，也不要在当前远距离小板的手眼数据上自由拟合全部内外参来吸收误差。

一旦依据验证结果调整模型、阈值或筛选规则，这批验证数据已经参与开发；需要另采新图作最终确认。确认后冻结内参文件、哈希和适用视频流配置。

## 7. 静止重复性测试

在大量手眼采集之前，固定棋盘和头部，在实际工作距离采约 100 帧。至少检查画面中央和偏边缘的位置；不同检查位置可以重新布置，但每段测试内部必须静止。

检查：

- 检测成功率与角点编号是否稳定。
- 角点像素坐标的波动。
- 固定内参下 PnP 原点三维位置的波动、姿态跳变和随时间漂移。
- 实测关节角和速度是否仍在变化。
- 图像曝光和亮度是否变化。

三维位置重复性可按各帧位置到本段均值的距离统计 RMS/Max。可先争取 RMS 约 1～2 mm 或更小，为其他误差留出预算。这是诊断目标，不是出厂指标；静止稳定也不证明不存在固定偏差。

如果静止时输出经常跳动超过 5 mm，应先定位视觉、PnP、机械或同步问题，再增加标定姿态。

## 8. 手眼训练采集

### 8.1 固定条件

在一个 session 内保持棋盘与基座相对静止，只运动头部 yaw/pitch。不能照搬内参采集阶段“拿着棋盘到处移动”的方法。

相机安装、基座或棋盘发生移动时，结束该 session。改变棋盘位置后可以新建 session，但求解时每个 session 必须有各自的固定板位姿，只共享同一个手眼矩阵。

### 8.2 姿态安排

先确定全部 88 个内角点清晰可见的 yaw/pitch 范围，再在其中分散选点：

1. 可先按约 7 个 yaw 水平 × 5 个 pitch 水平安排 35 个组合。
2. 视野不允许的组合改为附近的可见组合，不能为了凑网格裁掉棋盘。
3. 再补约 10 个非网格组合，覆盖较少采到的区域。
4. 让两个轴独立变化，避免只转一个轴，或两个轴始终按固定比例联动。
5. 在条件允许时交错采集不同区域，并回访参考姿态，以检查随时间变化的误差。

当前两轴机构不必因为没有 roll 就判定不可用。更大的角度必须服从真实可见范围、清晰度和机构允许范围；不指定未经现场验证的绝对关节命令。

### 8.3 每个姿态的采集顺序

```text
到达姿态 → 观察实测关节状态稳定 → 检查图像质量
        → 保存 10～20 帧原图及逐帧对应状态 → 记录质量 → 下一姿态
```

- 可以先观察约 0.5～1 秒稳定窗口，再触发采集；最终以实际状态稳定为依据。
- 固定等待时间不能证明已停稳。建议保存窗口内角度变化和速度，以便检查。
- 保存实测状态，不使用目标角或指令角代替。
- 保留 q_raw、q_ours、关节名称、单位和方向映射。当前 pitch 方向转换应沿用已经核实的约定，不能重复取反。
- 保存 FK 使用的全部活动关节状态；缺失状态不能无依据默认为零。
- 每帧保留图像曝光/设备时间、关节采样时间、主机接收时间及各自时钟域。不同时间域不能直接相减或当作同一时间使用。
- 静止采集能降低时间误差影响，但不能替代对时间对应关系的记录和验证。

多帧用于诊断重复性，或在事先规定的静止融合方法下改善随机噪声。不要直接平均欧拉角，也不要为了使残差最小而事后挑选某一帧。

## 9. 独立验证与回访采集

建议在参数冻结后新采约 25 个验证姿态：

- 在训练姿态之间和实际工作范围边缘附近选不同组合。
- 覆盖真实任务使用的 yaw/pitch、距离和画面区域。
- 不复用训练图像，不把训练姿态的连拍帧拆过来当独立验证。
- 每个姿态同样保留 10～20 帧，记录所有失败。
- 若实际应用单帧定位，必须报告单帧结果；若使用多帧融合，验收使用相同策略，并记录窗口和延迟。

另选约 5 个参考姿态，离开后再重新到达并采集。可以比较不同到达方向的结果，检查机械回差、重复性和温漂；不能从少数回访结果推断整个空间都已通过。

若改变板位置验证其他工作距离，每个板位置须有独立参考位姿。相对外部真值验证时使用已测量的参考；仅做一致性检查时，可以用预先分离的参考观测固定该位置的板位姿，再评价其余观测，但应明确此时不是绝对精度验收。

## 10. 原始数据组织与记录字段

建议结构：

```text
capture_YYYYMMDD/
├── camera_sdk.json             # 实际视频流、原始 K/D/模型、序列号
├── intrinsics_selected.json    # 最终选定内参、来源和适用配置
├── board.json                  # 方格数、内角点数、实测格距
├── robot_model.urdf
├── session.json                # 固定坐标系、环境、版本与分组规则
├── intrinsic/
│   ├── sdk_check/
│   ├── train/                  # 仅重标定时需要
│   └── validation/             # 仅重标定时需要
├── static_check/
└── handeye/
    └── session_001/
        ├── train/pose_000/frame_000.png
        ├── validation/pose_000/frame_000.png
        ├── revisit/
        └── records.jsonl
```

每帧记录至少包含：

| 类别 | 内容 |
|---|---|
| 身份 | session_id、pose_id、frame_id、相对图片路径、分组 |
| 图像 | 宽高、编码、原图/去畸变状态、文件哈希 |
| 相机 | 序列号、内参版本/哈希、曝光和增益设置 |
| 时间 | 图像时间戳及时间域、关节采样时间及时间域、主机接收时间 |
| 机器人 | 完整关节名称、实测 q_raw/q_ours、单位、方向映射 |
| 运动学 | 基座与末端名称、URDF 文件版本/哈希 |
| 位姿 | 对应时刻的 T_base_head，变换方向、平移单位；原始关节状态同时保留 |
| 棋盘 | 内角点行列、实测格距、固定板位置编号 |
| 质量 | 检测成功与否、角点间距、重投影误差、稳定性、拒收原因 |

优先同时保存原始关节状态和 4×4 位姿矩阵。如果保存四元数，明确分量顺序；如果保存 RPY，明确旋转顺序、内/外旋和角度单位。

### 10.1 导出给当前 C++ 程序

当前程序要求：

```text
dataset/
├── rgb/
│   ├── train_0000.png
│   ├── ...
│   ├── val_0000.png
│   └── ...
├── poses.csv
└── intrinsics_verified.xml
```

CSV 每张图一行，图像名必须与程序排序后的图片列表逐一对应：

```text
# image_name,capture_stamp_s,x_mm,y_mm,z_mm,rx_deg,ry_deg,rz_deg
```

CSV 位姿为 `T_waist_yaw_Link_head_pitch_Link`；平移 mm；RPY 为固定轴 XYZ、单位 deg。时间戳使用什么时钟域，应在配套元数据中注明。

建议每个独立姿态按事先确定的规则导出一张代表图，例如稳定连拍段的固定序号帧；全部原始帧仍保留。更复杂的融合需要明确实现，不能直接把大量相关帧当成更多独立姿态。

当前 `calib.cpp` 将排序后最后 N 张图片留出验证。使用补零文件名并核查排序，使训练文件在前、验证文件在后；CSV 使用完全相同的顺序。`--validate-count` 是留出图片数，不是原始连拍帧数或自动识别的姿态组数。

内参 XML 使用当前程序读取的字段 `K`、`distortion`，并保存 width、height、模型、来源等配套元数据。不要在 XML 中填写示例焦距或未经核实的零畸变。

## 11. 质量检查与异常处理

| 检查 | 处理 |
|---|---|
| 图片与内参分辨率不一致 | 暂停使用，核实配置；不能默认缩放可修复 |
| 原图被裁剪/缩放/去畸变但无记录 | 查明处理链及对应内参后再使用 |
| 棋盘不完整、模糊、反光或过曝 | 记录原因，调整条件并重新采集 |
| 相邻角点过小 | 调整距离/成像条件；不能用图像放大冒充更高采样精度 |
| 重复图像或重复姿态大量堆积 | 保留原始记录，不作为更多独立观测计数 |
| 时间配对不明、关节状态缺失 | 标记无效，修正采集后重采 |
| 角点物理原点翻转、PnP 姿态跳变 | 核对对应关系、正深度和候选解 |
| 棋盘或基座移动 | 新建 session，不当成原固定板批次直接拼接 |
| 验证误差大 | 报告失败，定位原因；不得事后删除该点宣布通过 |

检测失败或质量拒收都应计数并保存原因。训练集可按事先规定的规则筛选；验证集不能依据结果大小挑点。

当前经典角点检测分支使用 `cornerSubPix` 半窗口 11×11，实际覆盖 23×23 px。对于小棋盘，应核查是否覆盖邻近角点；统一可靠的亚像素检测或按角点间距设置窗口。检测成功不等于定位足够准确。

## 12. 求解流程与现有程序的使用边界

1. 核对单位、坐标方向、图像与姿态配对，以及每个 session 的固定板假设。
2. 固定已验证的 RGB 内参，核验角点身份和 PnP 结果。
3. 使用 PARK 等方法获得初值，并比较其他方法及逐帧残差；方法分歧是诊断信号，不能直接等同于运动不可观测。
4. 必要时只用训练数据联合优化手眼矩阵与固定板位姿，最小化角点重投影误差；鲁棒损失和参数设置须记录。
5. 冻结结果与处理规则，再执行独立验收。

仓库现有程序可通过 `--fixed-intrinsics` 指定固定 K、D。假设已经导出 45 张训练代表图和 25 张验证代表图、排序和 CSV 完全一致，并且相机模型处理正确，则命令示例为：

```bash
./build/calib \
  --dataset ./dataset \
  --output ./calibration_output_new \
  --fixed-intrinsics ./dataset/intrinsics_verified.xml \
  --validate-count 25
```

这条命令不是完整验收自动化，也不表示当前 C++ 主程序已经集成全部联合优化和全角点评价。

**非零畸变的当前实现注意事项：** `BoardImageCalibrator.cpp` 先调用 `undistort`，固定内参路径随后又把原始 D 传给 PnP；当前留出验证也采用类似处理。D 非零时，运行前应修正为一致的“原图 + 原始 K,D”或“去畸变图 + 对应新 K,零 D”，不能直接把非零 D 填入上述命令后忽略这一问题。旧数据 D 为零并不能替代新视频流的核实。

实际格距需要小数毫米时，也应检查 `CalibConfig.interval_mm` 当前为整数的限制。本文只记录这些前置条件，不修改求解代码。

## 13. 5 mm 验收方法

### 13.1 固定板一致性

定义：

- G_i = T_base_head：第 i 个姿态的头部末端到基座变换。
- X = T_head_camera_optical：求得的手眼变换。
- C_i = T_camera_optical_board：验证图片独立求得的板到相机变换。
- Y_train = T_base_board：仅由训练数据确定的固定板位姿。

理想情况下：

```text
G_i X C_i = Y_train
```

对板上的已知点 p_j，计算：

```text
e_ij = norm( xyz(G_i X C_i p_j) - xyz(Y_train p_j) )
```

点坐标与所有平移使用一致的 mm 单位；p_j 使用齐次坐标。报告板原点和全部 88 个角点误差，避免只检查原点而忽略姿态偏差。

最终验证不能重新拟合 X、K、D，也不能用验证帧重新估计参考中心来压低误差。预先定义评价点集合及单帧/融合方式；例如以所有有效验证姿态、全部预定义角点的最大误差作为 5 mm 判据，并同时报告失败情况。

当前 C++ 日志主要报告板原点一致性，全角点和外部真值检查需要另行完成。程序输出 `SUCCESS` 只表示求解/流程完成，不表示已满足 5 mm。

### 13.2 绝对定位精度

如果要求相对于机器人真实坐标的误差 ≤5 mm，需要位置已知、测量不确定度明显小于目标误差的外部参考，并记录参考测量方法及不确定度。

可以使用具有可靠基座坐标的测量点或标定工装。若使用机器人 TCP 接触作为参考，必须先确认 TCP、机器人绝对定位和接触测量的精度；不能把未经验证的机器人读数当成无误差真值。

参考点应覆盖实际工作范围和距离。仅用训练数据估计出的板位置作参考，或者只看投影覆盖得是否漂亮，都不构成绝对精度证明。

### 13.3 深度点云或抓取任务

最终定位若使用深度数据，应在实际近、中、远工作距离分别检查：

- 深度值相对已知距离/几何的固定偏差与重复性。
- 深度尺度、无效值和所用滤波策略。
- RGB 与深度内外参、对齐方式及坐标系。
- 物体或目标点经过完整变换链后的三维误差。

Z16 深度建议原始无损保存，并保留 depth_scale、深度内参、RGB/深度外参、时间戳、对齐与滤波状态。不能只保存彩色化深度图。深度范围和量纲要区分光轴 Z 与到相机中心的欧氏距离。

RGB 棋盘格 PnP 通过，不能替代深度点云或实际抓取的验收。

### 13.4 验收报告

| 报告项 | 内容 |
|---|---|
| 配置 | 相机/视频流、内参来源和哈希、棋盘实测值、URDF 版本 |
| 数据量 | 独立训练/验证姿态数、每姿态帧数、失败数 |
| 图像质量 | 棋盘像素尺寸、各图重投影误差、异常原因 |
| 一致性结果 | 原点和全角点三维 RMS/Max、XYZ 误差 |
| 覆盖范围 | 工作距离、yaw/pitch、画面区域及各区域结果 |
| 重复性 | 静止连拍、回访姿态、到达方向相关结果 |
| 绝对误差 | 外部参考及其不确定度、逐点误差；未测则明确写未测 |
| 深度结果 | 最终使用深度时单独列出 |
| 结论 | 是否满足预定义的 5 mm 判据，适用范围和未验证范围 |

约 25 个验证姿态只能支持所覆盖工况的结论，不保证全部未测位置通过。失败点不能静默删除；若根据失败结果修改了方案，应冻结新版本并重新采最终验证数据。

## 14. 未达标时的排查顺序

| 现象 | 优先排查 |
|---|---|
| 静止也有明显跳动 | 图像清晰度、角点尺寸、PnP 多解、机械振动 |
| 边缘误差明显更大 | 内参、畸变模型、角点质量、图像处理链 |
| 与某个关节角或到达方向相关 | 关节正负号、零位、轴线/URDF、回差或结构变形 |
| 随时间漂移 | 温度、安装或棋盘移动、同步与采集状态变化 |
| 训练好、独立验证差 | 覆盖不足、重复数据、过拟合或模型不一致 |
| RGB/PnP 尚可、深度点云差 | 深度偏差、尺度、RGB/深度配准和完整坐标链 |

不要先靠增加数百个相似姿态、放开全部内参或删除大误差验证点来处理问题。

## 15. 现场执行清单

- [ ] 实际原图为 1280×720，未缩放/裁剪，编码明确。
- [ ] 读取了实际 RGB profile 的 K、畸变模型和系数，未套用旧配置。
- [ ] 核对发布器的零畸变与 SDK 原始值是否一致。
- [ ] 12×9 方格对应 11×8 内角点，实测格距和板平整度已记录。
- [ ] 实际像素间距和棋盘尺寸足够，边缘清楚且全部内角点可见。
- [ ] 先用约 10～15 张图验证 SDK 内参；只有需要时才独立重标定。
- [ ] 内参已经冻结，图像处理与畸变模型一致。
- [ ] 静止测试已完成，重复性和异常情况有记录。
- [ ] 手眼 session 内棋盘与基座固定，相机安装未变化。
- [ ] 训练姿态覆盖两轴变化，实测状态与每帧图像配对。
- [ ] 连拍帧按姿态分组，原始数据和时间域完整保存。
- [ ] 约 25 个独立验证姿态未参与参数选择或拟合。
- [ ] 原图、原始关节状态、K/D、URDF 和版本信息已归档。
- [ ] 导出图像排序、CSV 配对和验证数量已经核对。
- [ ] 报告最大三维误差和失败数量，没有按误差大小删除验证点。
- [ ] 绝对定位/深度点云任务已完成相应额外验收，或明确记录尚未验证。
